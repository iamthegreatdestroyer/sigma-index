cmake_minimum_required(VERSION 3.16)
project(simple_test)

find_package(pybind11 REQUIRED)

pybind11_add_module(simple_test simple_test.cpp)

set_target_properties(simple_test PROPERTIES
    LIBRARY_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}
)