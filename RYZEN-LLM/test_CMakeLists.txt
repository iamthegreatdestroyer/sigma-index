cmake_minimum_required(VERSION 3.16)
project(test_bindings)

find_package(pybind11 REQUIRED)

pybind11_add_module(test_bindings test_bindings.cpp)

set_target_properties(test_bindings PROPERTIES
    LIBRARY_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}
)